import './styles.scss';  // Hubungkan file styles.scss ke sini
import * as bootstrap from 'bootstrap';  // Import semua fitur Bootstrap

document.addEventListener('DOMContentLoaded', () => {
    console.log('Website siap digunakan!');
});
